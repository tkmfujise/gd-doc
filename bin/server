#!/usr/bin/env ruby

Dir.chdir File.expand_path('..', __dir__)


# To handle Ctrl-C expected
trap :INT do
  pid = `pgrep -u #{Process.uid} -f puma`
  Process.kill(:TERM, pid.to_i) if pid
  exit
end


system 'bundle exec nanoc live --host 0.0.0.0'
